// Also store list, to be able to efficiently remove it later on
var $sections = $('.split-list > ul');
var $items = $('.split-list * li');
var totalsections = Math.floor($sections.length);
var totalitems = Math.floor($items.length);

for (var i=1; i <= totalsections; i++) {
  $totalsections.eq(i*3).parentElement.parentelement.after('<hr class="gl_hr">');  
}